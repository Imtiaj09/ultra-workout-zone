import React from 'react';

const Product = (props) => {
  const { name, Time required, For age, img } = props.product
  return (
    <div>
      <h2>This is product</h2>
    </div>
  );
};

export default Product;